const paths = require('./paths');

module.exports = {
  config: `${paths.config}`,
  app: `${paths.src}`,
  houseRental: `${paths.src}/houseRental`,
  common: `${paths.src}/common`,
  static: `${paths.src}/public`,
};
